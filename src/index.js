import React from 'react';
import ReactDOM from 'react-dom/client';
import ThinkAgainApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ThinkAgainApp />
  </React.StrictMode>
);
